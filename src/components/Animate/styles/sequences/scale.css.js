const css = `
&.ax-scale {
  transform: scale(0.8);

  &.ax-entered {
    transform: scale(1);
  }
}

&.ax-scaleLg {
  transform: scale(0.5);

  &.ax-entered {
    transform: scale(1);
  }
}

&.ax-scaleXl {
  transform: scale(0.3);

  &.ax-entered {
    transform: scale(1);
  }
}
`

export default css
