import styled from 'styled-components'

export const DropUI = styled.div`
  left: 0;
  position: absolute;
  top: 0;
  font-family: var(--HSDSGlobalFontFamily);
  font-size: var(--HSDSGlobalFontSize);
`
export const DropContentUI = styled.div`
  display: inline-block;
`
