import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import { boolean } from '@storybook/addon-knobs'
import {
  ItemSpec,
  itemsWithDivider,
  groupedItems,
} from '../../utilities/specs/dropdown.specs'
import Dropdown from './'
import { Dropdown as DropdownUnconnected } from './Dropdown'

<Meta
  title="🧬 Components/Dropdowns/Dropdown"
  component={Dropdown}
  parameters={{
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/Wp9mDxTvWicSTWtezj2ImkMP/HSDS-Product?node-id=2582%3A8006',
    },
  }}
/>

# Dropdown

A Dropdown component is provides the UI to contain a series of actions, presented in a menu overlay.

<Preview>
  <Story name="default">
    <div style={{ height: '200px' }}>
      <Dropdown
        items={ItemSpec.generate(5)}
        disabled={boolean('disabled', false)}
        isOpen
      />
    </div>
  </Story>
</Preview>

#### Reference

- **Designer**: Buzz
- **Engineering**: Juan Pablo
- **Usage**: Everywhere

<br />
<br />

### Props

<Props of={DropdownUnconnected} />

### Stories

#### Empty

<Preview>
  <Story name="Empty">
    <div style={{ height: '70px' }}>
      <Dropdown
        items={[]}
        renderEmpty={() => <div>Nothing here!</div>}
        trigger="click to open"
      />
    </div>
  </Story>
</Preview>

#### With dividers

<Preview>
  <Story name="With dividers">
    <div style={{ height: '250px' }}>
      <Dropdown items={itemsWithDivider} trigger="click to open" />
    </div>
  </Story>
</Preview>

#### Items in groups

<Preview>
  <Story name="Items in groups">
    <div style={{ height: '250px' }}>
      <Dropdown items={groupedItems} trigger="click to open" />
    </div>
  </Story>
</Preview>

#### Multiple Selected Items

<Preview>
  <Story name="Multiple selected items">
    <div style={{ height: '250px' }}>
      <Dropdown
        items={itemsWithDivider}
        selectedItem={[...itemsWithDivider].splice(2, 3)}
      />
    </div>
  </Story>
</Preview>
