import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks'
import { select } from '@storybook/addon-knobs'
import { action } from '@storybook/addon-actions'
import EmojiPicker from './'

<Meta
  title="Components/Conversation/EmojiPicker"
  component={EmojiPicker}
  parameters={{
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/Wp9mDxTvWicSTWtezj2ImkMP/HSDS-Product?node-id=2582%3A7743',
    },
  }}
/>

# EmojiPicker

The EmojiPicker component contains the logic to present a set of emoji in a menu for selection.

<Canvas>
  <Story name="default">
    <div style={{ paddingTop: '50px', marginLeft: '300px' }}>
      <EmojiPicker
        size={select(
          'Emoji Size',
          { default: 'default', sm: 'sm', lg: 'lg' },
          'default'
        )}
        shouldRefocusOnClose={() => false}
        onSelect={action('Emoji Selected')}
      />
    </div>
  </Story>
</Canvas>

#### Reference

- **Designer**: Buzz
- **Engineering**: Terrence
- **Usage**: HSApp (ChatApp)

<br />
<br />

### Props

<ArgsTable of={EmojiPicker} />
