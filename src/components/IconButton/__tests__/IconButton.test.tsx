// import * as React from 'react'
// import { mount, render } from 'enzyme'
// import { IconButton } from '../IconButton'

// describe('className', () => {
//   test('Has default className', () => {
//     const wrapper = render(<IconButton />)

//     expect(wrapper.hasClass('c-IconButton')).toBeTruthy()
//   })

//   test('Can render custom className', () => {
//     const customClassName = 'blue'
//     const wrapper = render(<IconButton className={customClassName} />)

//     expect(wrapper.hasClass(customClassName)).toBeTruthy()
//   })
// })

// describe('HTML props', () => {
//   test('Can render default HTML props', () => {
//     const wrapper = render(<IconButton data-cy="blue" />)

//     expect(wrapper.attr('data-cy')).toBe('blue')
//   })
// })
