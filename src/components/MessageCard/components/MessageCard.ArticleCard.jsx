import React from 'react'
import { MessageArticleCardUI } from '../MessageCard.css'

function MessageCardArticleCard(props) {
  return <MessageArticleCardUI {...props} />
}

export default MessageCardArticleCard
