import styled from '../styled'
import baseStyles from '../../styles/resets/baseStyles.css.js'

export const NotificationStackUI = styled('div')`
  ${baseStyles};
  .is-theme-chat {
    max-width: 400px;
  }
`
