import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks'
import { select, number } from '@storybook/addon-knobs'
import StatusBadge from './'

<Meta title="🧬 Components/Badges/StatusBadge" component={StatusBadge} />

# StatusBadge

<Canvas>
  <Story name="default">
    <div>
      <StatusBadge
        count={number('count', 5)}
        status={select(
          'Status',
          {
            online: 'online',
            offline: 'offline',
            busy: 'busy',
            new: 'new',
            active: 'active',
            inactive: 'inactive',
          },
          'online'
        )}
      />
    </div>
  </Story>
</Canvas>

#### Reference

- **Designer**: Buzz
- **Engineering**: Juan Pablo
- **Usage**: Beacon2

<br />
<br />

### Props

<ArgsTable of={StatusBadge} />
