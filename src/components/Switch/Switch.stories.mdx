import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks'
import { select, boolean } from '@storybook/addon-knobs'
import Switch from './'

<Meta
  title="Components/Forms/Switch"
  component={Switch}
  parameters={{
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/Wp9mDxTvWicSTWtezj2ImkMP/HSDS-Product?node-id=2582%3A9631',
    },
  }}
/>

# Switch

A Switch component is an alternative to a `Checkbox` that provides a switch-toggle UI.

<Canvas>
  <Story name="default">
    <Switch
      checked={boolean('checked', false)}
      disabled={boolean('disabled', false)}
      isLoading={boolean('isLoading', false)}
      size={select(
        'Size',
        {
          lg: 'lg',
          md: 'md',
          sm: 'sm',
        },
        'md'
      )}
      state={select(
        'State',
        {
          error: 'error',
          default: '',
        },
        ''
      )}
    />
  </Story>
</Canvas>

#### Reference

- **Designer**: Buzz
- **Engineering**: Juan Pablo
- **Usage**: Messages, Beacon embed, Mailbox Settings

<br />
<br />

### Props

<ArgsTable of={Switch} />
