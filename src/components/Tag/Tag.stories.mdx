import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks'
import { boolean, select } from '@storybook/addon-knobs'
import Tag from './'

<Meta
  title="Components/Badges/Tag"
  component={Tag}
  parameters={{
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/Wp9mDxTvWicSTWtezj2ImkMP/HSDS-Product?node-id=2582%3A7443',
    },
  }}
/>

# Tag

A Tag component is a UI element that provide contextual labels for categories/tags.

<Canvas>
  <Story name="default">
    <div>
      <Tag
        color="red"
        filled={boolean('filled', false)}
        pulsing={boolean('pulsing', false)}
        size={select('Size', { md: 'md', sm: 'sm' }, 'sm')}
      >
        My tag
      </Tag>
      <br />
      <br />
      <Tag
        color="grey"
        filled={boolean('filled', false)}
        pulsing={boolean('pulsing', false)}
        size={select('Size', { md: 'md', sm: 'sm' }, 'sm')}
      >
        My tag
      </Tag>
      <br />
      <br />
      <Tag
        color="green"
        filled={boolean('filled', false)}
        pulsing={boolean('pulsing', false)}
        size={select('Size', { md: 'md', sm: 'sm' }, 'sm')}
      >
        My tag
      </Tag>
      <br />
      <br />
      <Tag
        color="blue"
        filled={boolean('filled', false)}
        pulsing={boolean('pulsing', false)}
        size={select('Size', { md: 'md', sm: 'sm' }, 'sm')}
      >
        My tag
      </Tag>
      <br />
      <br />
      <Tag
        color="orange"
        filled={boolean('filled', false)}
        pulsing={boolean('pulsing', false)}
        size={select('Size', { md: 'md', sm: 'sm' }, 'sm')}
      >
        My tag
      </Tag>
      <br />
      <br />
      <Tag
        color="purple"
        filled={boolean('filled', false)}
        pulsing={boolean('pulsing', false)}
        size={select('Size', { md: 'md', sm: 'sm' }, 'sm')}
      >
        My tag
      </Tag>
      <br />
      <br />
      <Tag
        color="yellow"
        filled={boolean('filled', false)}
        pulsing={boolean('pulsing', false)}
        size={select('Size', { md: 'md', sm: 'sm' }, 'sm')}
      >
        My tag
      </Tag>
      <br />
      <br />
      <Tag
        color="lightBlue"
        filled={boolean('filled', false)}
        pulsing={boolean('pulsing', false)}
        size={select('Size', { md: 'md', sm: 'sm' }, 'sm')}
      >
        My tag
      </Tag>
    </div>
  </Story>
</Canvas>

#### Reference

- **Designer**: Buzz
- **Engineering**: PL
- **Usage**: ChatApp, Customers

<br />
<br />
<br />
<br />

### Props

<ArgsTable of={Tag} />
