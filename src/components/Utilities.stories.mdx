import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="🛠 Utilities/1. Intro" />

# HSDS Utilities

A collection of components that provide advanced functionality like "drag and drop" (`Sortable`), "infinite scrolling" (`InfiniteScroller`), and others for convenience or repetitive tasks, like `Truncate` and `VisuallyHidden`.
